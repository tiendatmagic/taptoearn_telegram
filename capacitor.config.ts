import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tiendatmagic.taptoearn',
  appName: 'taptoearn',
  webDir: 'dist/taptoearn/browser',
};

export default config;
